version: '2'

services:
  node:
    image: 'node:8'
    user: "node"
    working_dir: /home/node/app
    command: "npm start"
    #environment:
    #  - NODE_ENV=production
    volumes:
      - '.:/home/node/app'
    ports:
      - '8000:8080'
